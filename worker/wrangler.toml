name = "financa-worker"
main = "index.js"
compatibility_date = "2025-01-01"

# Variáveis de ambiente — substitua com seus valores reais
# NUNCA comite a SUPABASE_KEY aqui. Use: wrangler secret put SUPABASE_KEY
[vars]
SUPABASE_URL = "https://ajsafogxhdlmswszhudo.supabase.co"
ALLOWED_ORIGIN = "https://chat-financas.pages.dev"

# Segredos — execute uma vez no terminal (pasta worker/):
# wrangler secret put SUPABASE_KEY   ← service_role key do Supabase
# wrangler secret put OPENAI_KEY     ← sk-... da OpenAI
